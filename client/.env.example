# Base URL of the backend API.
# Local development points at the Express dev server.
# In production (Vercel), set this to your Render URL, e.g.
# https://wealthpilot-api.onrender.com
VITE_API_URL=http://localhost:4000
